#:kivy 1.7.2

<ResultsScreen>:
    orientation: 'vertical'
    BoxLayout:
        size_hint: 1, .05
        pos_hint: {'x': 0, 'y': .95}
        Button:
            text: 'Usage Notes'
        Button:
            text: 'Something'
        Button:
            text: 'Home'
    BoxLayout:
        size_hint: 1, .1
        pos_hint: {'x': 0, 'y': .85}
        TextInput: 
            text: 'search again'
            background_color: .8, .8, 0, 1
            size_hint: .7, .95
            multiline: True
            pos_hint: {'x': .025, 'y': .025}
            text_size: self.size
            valign: 'middle'
            halign: 'center'
            padding: 5
        Button:
            text: 'Translate'
            pos_hint: {'x': .775, 'y': .025}
            size_hint: .2, .95
            valign: 'middle'
            halign: 'center'
            text_size: self.size  
            on_release: root.results_screen
    BoxLayout:
        size_hint: .9, .75
        pos_hint: {'x': .05, 'y': .1}
        orientation: 'horizontal'
        BoxLayout:
            orientation: 'vertical'
            size_hint: .75, 1
            Label:
                text: 'Result'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Result'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Result'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Result'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Result'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
        BoxLayout:
            orientation: 'vertical'
            size_hint: .25, 1
            pos_hint: {'x': .75, 'y': 0}
            Label:
                text: 'Link to search'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Link to search'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Link to search'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Link to search'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
            Label:
                text: 'Link to search'
                text_size: self.size
                valign: 'middle'
                halign: 'center'
    FloatLayout:
        size_hint: 1, .1
        Button:
            size_hint: .5, .5
            pos_hint: {'x': .25, 'y': .25}
            text: 'More hits'
            text_size: self.size
            valign: 'middle'
            halign: 'center'

<HomeScreen>:
    orientation: 'vertical'
    FloatLayout:
        size_hint: 1, .95    
        TextInput: 
        	text: 'funny stuff'
            background_color: .8, .8, 0, 1
            size_hint: .75, .1
            multiline: True
    		pos_hint: {'x': .125, 'y': .45}
    		text_size: self.size
            valign: 'middle'
            halign: 'center'
    		padding: 5
    		
        Button:
        	text: 'Translate'
        	pos_hint: {'x': .35, 'y': .35}
        	size_hint: .3, .08
            valign: 'middle'
            halign: 'center'
            text_size: self.size  
            on_release: root.results_screen  

        Label: 
        	text: '*Bedouin مُخْتَبَر*'
        	text_size: self.size
            valign: 'middle'
            halign: 'center'
        	pos_hint: {'x': .3, 'y': .75}
        	size_hint: .4, .2

    BoxLayout:
        size_hint: 1, .05
        pos_hint: {'x': 0, 'y': .95}
        Button:
            text: 'Usage Notes'
        Button:
            text: 'About'
        
    
